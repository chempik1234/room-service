1. simple protobuf
2. in-memory room implementation
    1. ports, repos
    2. rooms exist offline for TTL
3. handlers (stream for room lifecycle)
4. USER_ONLY_1_ROOM=bool
5. Query (e.g. list all rooms)
6. Tell if error caused by caller or is it internal
7. more...